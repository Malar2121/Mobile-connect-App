import React from 'react';
import { Avatar as DSAvatar } from '../design-system';

/** @deprecated Use Avatar from design-system directly */
export function Avatar(props) {
  return <DSAvatar {...props} />;
}
